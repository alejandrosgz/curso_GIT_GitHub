# install.packages("usethis")
# install.packages("gitcreds")

# library(usethis)
# library(gitcreds)


# use_git_config(user.name = "Jane Doe", user.email = "jane@example.org") permite configurar el nombre y usuario de git
# para hacer la configuración inicial desde rstudio en vez de desde git shell.

# create_github_token() # generar un token identificativo para el usuario de git-github

# gitcreds_set() # acceder al Git credential store --> introducir el token generado


